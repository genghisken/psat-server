Locate the CFITSIO libraries - usually in my installations they live in
    /usr/local/swtools/include
    /usr/local/swtools/lib

So...

export BASE=/usr/local/swtools
export INCDIR=${BASE}/include
export LIBDIR=${BASE}/lib

make

mkdir -p ${BASE}/bin

cp -p fits2jpeg ${BASE}/bin

